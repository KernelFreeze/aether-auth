// Package audit writes security-relevant events (login attempts, MFA
// challenges, password changes, session revocations, OAuth grants, org
// membership changes) to an append-only log.
package audit
