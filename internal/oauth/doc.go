// Package oauth hosts the OAuth2 authorization server (Fosite-backed) that
// issues scoped tokens via the authorization-code-with-PKCE and
// client-credentials grants. Implicit and password grants are not supported.
package oauth
