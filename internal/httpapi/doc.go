// Package httpapi is the HTTP wiring layer. It mounts each feature module's
// routes, composes middleware chains, and is the only place that knows the
// full URL map of the service.
package httpapi
